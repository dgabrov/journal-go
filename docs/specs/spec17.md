similar with postEditJournalItem

- add controller for POST /postAddJournalItem
- same payload as postEditJournalItem
- create in server.Server function AddJournalItem that will add journal item with all the checks of journal belonging to user  etc

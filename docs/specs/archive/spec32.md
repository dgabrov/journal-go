folder @docs/tests/files

- create a http file (intellij style) that would help me test the uploadFiles. Please create three files, jpeg, one png, one text in the same folder and point to them from the request. 
- then create a typescript file where you show me how to push the post from single page application. There is no need for authorization header as the auth will be done via cookie

I am very curious how to orchestrate the request from the front end, there is another project with that but you do not have access. 

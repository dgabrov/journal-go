Implement this:

- similar with other controllers like post_add_journal etc, create a new controller, POST, url /uploadFiles
- I need journalItemId, a string, passed to the request and multiple files
- it will probably be multipart 
- in the controller check the user is logged in
- check also that the user owns the journalItemId

then put a comment with TODO and I will tell you later the functionality

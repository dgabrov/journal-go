@docs/config/config.json 
- I added an entry called "files". This is for future files processing

- In ConfigData add those fields, create new type where needed and then ensure this is loaded there. Note the "dimension" is int
- Wherever you logged them, log the new values as well, in clear  all of them

